{% extends 'base.html.twig' %}

{% block title %}SafeCongo – Accueil{% endblock %}

{% block body %}
<div class="hero p-5 mb-4">
  <div class="row align-items-center g-4">
    <div class="col-lg-7">
      <h1 class="display-6 fw-bold mb-3">Signalez. Protégez. Sauvez des vies.</h1>
      <p class="lead mb-4">Plateforme citoyenne pour signaler incidents et dangers en temps réel en RDC. Recevez des alertes locales et consultez la carte des incidents.</p>
      <div class="d-flex gap-2">
        <a class="btn btn-primary btn-lg" href="{{ path('app_incident_report') }}">Signaler un incident</a>
        <a class="btn btn-outline-primary btn-lg" href="{{ path('app_map') }}">Voir la carte</a>
      </div>
    </div>
    <div class="col-lg-5">
      <div id="map" class="shadow-sm"></div>
    </div>
  </div>
</div>

<script>
// document.addEventListener('DOMContentLoaded', function () {
//   const map = window.SafeCongo && window.SafeCongo.initMap('map');
//   if (map) {
//     window.SafeCongo.locateAndCenter(map);
//   }
// });
</script>
{% endblock %}


