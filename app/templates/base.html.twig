<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}SafeCongo{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {# Bootstrap CSS #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

        {# Bootstrap Icons #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

        {# Leaflet CSS #}
        <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin=""/>

        {# Project CSS (compiled). Source in /public/styles.scss #}
        <link rel="stylesheet" href="{{ asset('styles.css') }}">

        {% block stylesheets %}{% endblock %}
    </head>
    <body class="bg-light">
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">SafeCongo</a>

                {# Desktop nav #}
                <ul class="navbar-nav ms-auto d-none d-lg-flex">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_incident_report') }}">Signaler</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_map') }}">Carte</a></li>
                </ul>

                {# Mobile toggler opens offcanvas instead of collapsing content (prevents layout shift) #}
                <button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNav" aria-controls="offcanvasNav" aria-label="Menu">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
        </nav>

        {# Offcanvas mobile menu #}
        <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNav" aria-labelledby="offcanvasNavLabel">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title" id="offcanvasNavLabel">Menu</h5>
                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" data-bs-dismiss="offcanvas" href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" data-bs-dismiss="offcanvas" href="{{ path('app_incident_report') }}">Signaler</a></li>
                    <li class="nav-item"><a class="nav-link" data-bs-dismiss="offcanvas" href="{{ path('app_map') }}">Carte</a></li>
                </ul>
            </div>
        </div>

        <main class="py-4">
            <div class="container">
                {% block body %}{% endblock %}
            </div>
        </main>

        <footer class="border-top bg-white mt-4">
            <div class="container py-5">
                <div class="row g-4">
                    <div class="col-12 col-md-6 col-lg-4">
                        <a href="{{ path('app_home') }}" class="text-decoration-none d-inline-flex align-items-center mb-2">
                            <span class="fs-5 fw-bold text-primary">SafeCongo</span>
                        </a>
                        <p class="text-muted mb-2">Plateforme d’alerte communautaire pour une sécurité publique renforcée en RDC.</p>
                        <div class="d-flex gap-3">
                            <a class="text-muted" href="#" aria-label="Twitter"><i class="bi bi-twitter-x fs-5"></i></a>
                            <a class="text-muted" href="#" aria-label="Facebook"><i class="bi bi-facebook fs-5"></i></a>
                            <a class="text-muted" href="#" aria-label="LinkedIn"><i class="bi bi-linkedin fs-5"></i></a>
                            <a class="text-muted" href="#" aria-label="YouTube"><i class="bi bi-youtube fs-5"></i></a>
                        </div>
                    </div>
                    <div class="col-6 col-lg-2">
                        <h6 class="text-uppercase text-muted small mb-3">Produit</h6>
                        <ul class="list-unstyled m-0">
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="{{ path('app_map') }}">Carte</a></li>
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="{{ path('app_incident_report') }}">Signaler</a></li>
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="#">Tableau de bord</a></li>
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="#">API</a></li>
                        </ul>
                    </div>
                    <div class="col-6 col-lg-3">
                        <h6 class="text-uppercase text-muted small mb-3">Ressources</h6>
                        <ul class="list-unstyled m-0">
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="#">Documentation</a></li>
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="#">Sécurité</a></li>
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="#">Support</a></li>
                            <li class="mb-2"><a class="text-decoration-none text-reset" href="#">Accessibilité</a></li>
                        </ul>
                    </div>
                    <div class="col-12 col-lg-3">
                        <h6 class="text-uppercase text-muted small mb-3">Contact</h6>
                        <ul class="list-unstyled m-0 text-muted">
                            <li class="mb-2"><i class="bi bi-envelope me-2"></i>contact@safecongo.cd</li>
                            <li class="mb-2"><i class="bi bi-telephone me-2"></i>+243 000 000 000</li>
                            <li class="mb-2"><i class="bi bi-geo-alt me-2"></i>Kinshasa, RDC</li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="bg-light">
                <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center py-3 small text-muted">
                    <div>&copy; {{ "now"|date('Y') }} SafeCongo. Tous droits réservés.</div>
                    <div class="d-flex gap-3">
                        <a class="text-reset text-decoration-none" href="#">Termes</a>
                        <a class="text-reset text-decoration-none" href="#">Confidentialité</a>
                        <a class="text-reset text-decoration-none" href="#">Cookies</a>
                    </div>
                </div>
            </div>
        </footer>

        {# Bootstrap JS bundle #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

        {# Leaflet JS #}
        <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>

        {# Project JS #}
        <script src="{{ asset('script.js') }}"></script>

        {% block javascripts %}{% endblock %}
    </body>
</html>
